package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMusicChartResourceIT extends MusicChartResourceTest {

    // Execute the same tests but in native mode.
}